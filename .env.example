# Rename to .env and set your keys
BINANCE_API_KEY=
BINANCE_API_SECRET=
# Set to 1 to use mainnet by default (otherwise testnet unless --mainnet flag)
BINANCE_MAINNET=0
# Optional: default leverage to set for a symbol
DEFAULT_LEVERAGE=5